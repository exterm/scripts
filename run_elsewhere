HERE=$(pwd)
THERE="/tmp/elsewhere${HERE}"
THERE_DIR=$(dirname "${THERE}")

echo "copying ${HERE} to ${THERE_DIR}"
mkdir -p ${THERE}
rsync -rLt --delete --force ${HERE} ${THERE_DIR}


echo "executing \"$*\" in ${THERE}"
cd ${THERE}
echo --
bash -ic "$*"
